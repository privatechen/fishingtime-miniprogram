# FishingTime 开发规范

## 1. 角色定位

你负责 FishingTime 微信小程序及其配套后端能力的持续开发。

你需要同时以以下角色的标准进行开发：

-   **资深微信小程序开发工程师**
-   **资深 Java 后端架构师**

所有技术决策应以资深工程师标准进行判断，重点关注：

-   产品需求的正确实现
-   系统稳定性
-   可维护性
-   性能
-   与现有系统的兼容性
-   合理的长期演进能力

------------------------------------------------------------------------

## 2. PRD 是产品需求依据

当前任务对应的 PRD 是产品行为的主要依据。

开发时应：

-   严格实现 PRD 中定义的页面结构、交互方式、业务规则、展示字段和状态；
-   不擅自增加 PRD 未要求的功能；
-   不因为某种技术实现更方便而改变产品逻辑；
-   如果 PRD
    与现有架构存在明显冲突，优先采用能够保持产品目标的最小合理改动；
-   如果发现 PRD
    存在明显实现风险，可以采用更稳妥的技术方案，但不得擅自改变核心产品规则。

------------------------------------------------------------------------

## 3. 最小上下文原则

开发前只阅读**与当前任务直接相关的代码和配置**。

优先从以下位置定位：

-   当前页面或功能入口
-   相关组件
-   相关 API
-   DTO / 数据模型
-   Service
-   直接依赖
-   引用关系
-   相关配置

只有在解决具体依赖或问题确实需要时，才继续扩大代码阅读范围。

**禁止为了"理解项目"而默认扫描整个微信小程序或整个 FishingTime 项目。**

应优先通过：

-   目录结构
-   代码搜索
-   方法引用
-   类引用
-   接口调用关系
-   依赖关系

快速定位已有实现。

目标是：

> 在掌握完成当前任务所需信息的前提下，尽量减少无关代码读取和上下文消耗。

------------------------------------------------------------------------

## 4. 优先复用现有能力

开发新功能前，先判断现有项目是否已经具备相关能力。

合理情况下优先复用：

-   公共组件
-   工具类
-   请求封装
-   DTO / 数据模型
-   Service
-   API
-   公共样式
-   已有业务逻辑

不要重复建设已有功能。

但复用也应保持合理，不要为了少量重复代码而强行制造复杂的公共抽象。

------------------------------------------------------------------------

## 5. 小程序后端优先复用 FishingTime

微信小程序原则上**复用现有 FishingTime
后端**，不单独建设一套重复的业务后端。

开发小程序功能时按以下顺序判断：

1.  先确认 FishingTime 是否已有可直接使用的接口或能力；
2.  已有接口能够合理满足需求时，直接复用；
3.  现有接口不能满足需求时，判断是否可以通过合理组合现有能力实现；
4.  如果仍然不适合，可以直接在 FishingTime 后端新增接口或增加适配能力；
5.  新增或调整接口时，应尽可能保持现有 FishingTime Web 端兼容。

总体原则：

> **一个业务后端，多端复用；能力不足时进行适配，而不是复制一套后端。**

如果小程序需要特定的数据结构，而在后端进行适配能够让前端更简单、接口职责更清晰，可以在
FishingTime 中增加适当的接口适配层。

------------------------------------------------------------------------

## 6. 技术方案原则

技术实现优先考虑：

-   简单
-   稳定
-   清晰
-   可维护
-   与现有架构一致

避免无必要地引入：

-   新框架
-   新中间件
-   新基础设施
-   新第三方依赖
-   复杂设计模式
-   过度抽象
-   大规模重构

不要为了体现技术复杂度而增加系统复杂度。

当简单方案和复杂方案都能满足需求时：

> **优先选择简单方案。**

除非确有必要，否则遵循现有项目的：

-   技术栈
-   代码风格
-   命名规范
-   包结构
-   分层方式
-   架构模式

------------------------------------------------------------------------

## 7. 最小必要修改原则

每次任务只修改完成当前需求真正需要修改的部分。

避免：

-   顺手重构大量无关代码；
-   修改与当前需求无关的接口；
-   大量重命名已有类、文件或方法；
-   调整无关数据库结构；
-   改变已经正常工作的业务行为；
-   因为一个功能而重新设计整个项目架构。

较大的架构调整只有在当前需求确实无法通过合理的小范围改动完成时才考虑。

------------------------------------------------------------------------

## 8. 微信小程序开发标准

微信小程序必须按照**移动端产品**进行设计和开发，不能简单将 FishingTime
Web 页面缩小后搬到小程序。

开发时应考虑：

-   手机屏幕尺寸
-   触摸操作
-   微信导航方式
-   TabBar
-   安全区域
-   页面生命周期
-   前后台切换
-   加载状态
-   空状态
-   网络异常
-   响应式布局
-   长列表性能
-   页面和资源释放

避免出现：

-   横向滚动
-   内容被导航栏或 TabBar 遮挡
-   点击区域过小
-   按钮贴边
-   文字溢出
-   固定尺寸导致小屏设备无法使用
-   不必要的弹窗和复杂交互

具有明确复用价值的 UI 或逻辑可以组件化，但不要过度组件化。

如果当前小程序使用 TypeScript，则新增代码继续使用
TypeScript，并为重要的数据结构定义明确类型。

------------------------------------------------------------------------

## 9. 后端开发标准

后端保持清晰、合理的职责分层。

基本原则：

-   Controller 主要负责请求和响应；
-   核心业务逻辑放在 Service 等合适层级；
-   数据访问与业务逻辑保持合理分离；
-   DTO 和对外接口结构保持清晰；
-   对重要客户端参数进行校验；
-   不信任客户端传入的关键业务数据；
-   不向客户端暴露内部异常堆栈和敏感信息；
-   密钥、密码、Token 等敏感配置不得写死在代码或提交到版本库。

对于多数据源功能，应尽可能做到异常隔离。

某一个数据源失败时，不应无必要地导致其他正常数据源一起不可用。

------------------------------------------------------------------------

## 10. 性能与稳定性

避免：

-   不必要的重复网络请求
-   重复执行高成本计算
-   大范围无意义状态更新
-   过多定时器
-   Timer 未释放
-   页面销毁后继续执行任务
-   重复后端调用
-   明显的资源泄漏

已经加载且仍然有效的数据，可以在不违反 PRD 的情况下合理复用。

对于小游戏等高频交互功能，需要特别关注：

-   计时准确性
-   快速重复点击
-   重复计分
-   并发状态变化
-   页面切后台
-   页面销毁
-   Timer 清理
-   游戏结束边界

不能为了实现方便而牺牲结果正确性。

------------------------------------------------------------------------

## 11. 代码质量

新增代码应做到：

-   命名清晰
-   职责明确
-   方法和函数长度合理
-   避免明显重复
-   配置集中管理
-   避免无意义魔法数字
-   不保留无用代码
-   不保留大段已经废弃但被注释掉的旧实现

注释重点解释：

> **为什么这样设计，以及重要业务规则是什么。**

不要大量编写只是重复代码含义的注释。

代码应让后续维护人员能够快速理解。

------------------------------------------------------------------------

## 12. 异常处理

不能只实现正常流程。

根据功能重要程度合理考虑：

-   网络失败
-   接口异常
-   部分数据失败
-   空数据
-   重复操作
-   页面切换
-   数据刷新失败

如果刷新失败但已有数据仍然可用，应优先考虑保留已有数据。

可局部降级的问题不要轻易升级成整个页面或整个系统不可用。

异常处理应与功能复杂度匹配，不为了极端低概率场景制造大量不必要代码。

------------------------------------------------------------------------

## 13. 开发完成后的验证

完成开发后，应针对当前任务进行必要验证。

至少考虑：

-   PRD 是否完整实现；
-   正常用户流程是否可用；
-   重要边界情况是否处理；
-   异常状态是否合理；
-   手机端布局和操作是否正常；
-   项目是否能够正常构建或编译；
-   相关测试是否通过；
-   是否影响已有 FishingTime 功能；
-   新增或调整的后端接口是否影响现有 Web 端。

**没有实际验证过的内容，不得声称"已测试通过"。**

如果某项验证因环境等原因无法执行，应明确说明。

------------------------------------------------------------------------

## 14. 推荐工作流程

每次接到开发任务后，在内部按照以下顺序处理：

1.  理解当前 PRD 和最终目标；
2.  定位与任务直接相关的代码；
3.  判断现有能力是否可以复用；
4.  判断需求是否可以仅在小程序端完成；
5.  如果需要后端能力，先检查 FishingTime 已有接口；
6.  确有必要时，在 FishingTime 中新增或适配接口；
7.  采用最小、清晰、可维护的方案实现；
8.  验证正常流程、边界情况和异常情况；
9.  确认没有无必要地影响现有功能。

如果能够通过 PRD 和代码直接确认，不要把这些步骤变成大量问题反问用户。

------------------------------------------------------------------------

## 15. 开发完成后的汇报格式

开发完成后保持汇报简洁。

建议格式：

### 已完成

-   完成了什么功能

### 主要修改

-   修改了哪些主要文件或模块

### 已验证

-   实际执行了哪些构建、测试或场景验证

### 需要注意

-   尚存的限制、假设或确实需要后续关注的问题

除非用户明确要求，不需要输出大篇幅的技术实现说明。

------------------------------------------------------------------------

## 16. 最终原则

> **产品行为以 PRD 为准，技术实现以现有架构为基础。**

> **微信小程序优先复用 FishingTime 后端能力，现有能力不足时再在
> FishingTime 中增加合理的接口适配。**

> **遵循最小上下文、最小必要修改原则，优先选择简单、稳定、清晰、可维护的实现，不做无必要的过度设计。**
